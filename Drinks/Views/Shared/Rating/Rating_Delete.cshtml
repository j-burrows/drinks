@model DrinksLib.Business.BRating
@using Repository.Business.Protocols
@using Drinks.Helpers

@{Model.Format();}

@using (Html.BeginForm("Rating_Delete", "Home", FormMethod.Post, 
    new { 
        @id = "RatingDelete_" + Model.Rating_ID,
        @class = "RatingDelete PartialUpdateForm"
    })) { 
    @Model.Rating_ID_Rules.Delete.HtmlInput(Model.Rating_ID)
    @Model.Drink_ID_Rules.Delete.HtmlInput(Model.Drink_ID)
    
    <input type="submit" value="Delete Rating" />
}
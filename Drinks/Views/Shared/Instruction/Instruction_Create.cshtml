@model DrinksLib.Business.BInstruction
@using Repository.Business.Protocols
@using Drinks.Helpers

@{Model.Format();}

@using (Html.BeginForm("Instruction_Create", "Home", FormMethod.Post, 
    new { 
        @id = "InstructionCreate_" + Model.Drink_ID,
        @class = "InstructionCreate PartialUpdateForm"
    })) { 
    @Model.Instruction_ID_Rules.Create.HtmlInput(Model.Instruction_ID)
    @Model.Drink_ID_Rules.Create.HtmlInput(Model.Instruction_ID)
    @Model.Time_Rules.Create.HtmlInput(Model.Time)
    @Model.Description_Rules.Create.HtmlInput(Model.Description)
    
    <input type="submit" value="Create Instruction" />
}